apiVersion: v1
kind: Service
metadata:
  name: myrecipe-backend
spec:
  type: NodePort
  selector:
    app: myrecipe-backend
  ports:
    - protocol: TCP
      port: 8080          # クラスタ内のServiceポート
      targetPort: 8080     # Podのコンテナポート
      nodePort: 30080  # 外部からアクセスするポート