apiVersion: v1
kind: Service
metadata:
  name: myrecipe-frontend
spec:
  type: NodePort
  selector:
    app: myrecipe-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32000  # 外部からアクセスするポート
